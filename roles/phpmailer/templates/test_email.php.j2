<?php
/**
 * PHPMailer Test Script for Azure Communication Services
 * 
 * This script tests the email sending functionality using Azure SMTP.
 * Use this to verify your configuration is working correctly.
 * 
 * WARNING: Remove this file in production environments!
 * 
 * Generated by Ansible
 */

// Include the mailer configuration
require_once '{{ phpmailer_install_dir }}/mailer_config.php';

$page_title = "PHPMailer Azure Email Test";
$test_result = null;
$error_message = null;

// Handle form submission
if ($_SERVER['REQUEST_METHOD'] === 'POST' && isset($_POST['send_test'])) {
    $recipient = filter_var($_POST['recipient'], FILTER_VALIDATE_EMAIL);
    
    if ($recipient) {
        try {
            $subject = "Test Email from PHPMailer - " . date('Y-m-d H:i:s');
            $body = "
                <html>
                <head>
                    <style>
                        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
                        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
                        .header { background: #667eea; color: white; padding: 20px; text-align: center; border-radius: 5px 5px 0 0; }
                        .content { background: #f9f9f9; padding: 20px; border: 1px solid #ddd; border-radius: 0 0 5px 5px; }
                        .info { background: #e3f2fd; padding: 15px; border-left: 4px solid #2196f3; margin: 15px 0; }
                        .footer { text-align: center; margin-top: 20px; color: #666; font-size: 12px; }
                    </style>
                </head>
                <body>
                    <div class='container'>
                        <div class='header'>
                            <h1>✉️ Test Email</h1>
                        </div>
                        <div class='content'>
                            <h2>PHPMailer Test - Azure Communication Services</h2>
                            <p>This is a test email sent from your LAMP stack using PHPMailer and Azure Communication Services SMTP relay.</p>
                            
                            <div class='info'>
                                <strong>📋 Test Details:</strong><br>
                                <strong>Sent at:</strong> " . date('Y-m-d H:i:s') . "<br>
                                <strong>Server:</strong> " . gethostname() . "<br>
                                <strong>PHP Version:</strong> " . phpversion() . "<br>
                                <strong>SMTP Host:</strong> " . AZURE_SMTP_HOST . "<br>
                                <strong>SMTP Port:</strong> " . AZURE_SMTP_PORT . "
                            </div>
                            
                            <p>If you received this email, your PHPMailer configuration with Azure Email Services is working correctly! 🎉</p>
                            
                            <p><strong>Next Steps:</strong></p>
                            <ul>
                                <li>Review the configuration in <code>{{ phpmailer_install_dir }}/mailer_config.php</code></li>
                                <li>Check the example script at <code>/send_email_example.php</code></li>
                                <li>Implement email functionality in your application</li>
                                <li><strong>Remove test scripts in production!</strong></li>
                            </ul>
                        </div>
                        <div class='footer'>
                            <p>Sent via PHPMailer with Azure Communication Services</p>
                        </div>
                    </div>
                </body>
                </html>
            ";
            
            $altBody = "This is a test email sent from your LAMP stack using PHPMailer and Azure Communication Services.\n\n";
            $altBody .= "Sent at: " . date('Y-m-d H:i:s') . "\n";
            $altBody .= "Server: " . gethostname() . "\n";
            $altBody .= "If you received this email, your configuration is working correctly!";
            
            $result = sendEmail($recipient, $subject, $body, $altBody);
            
            if ($result) {
                $test_result = "success";
            } else {
                $test_result = "error";
                $error_message = "Failed to send email. Check the logs at {{ mail_log_path }}";
            }
            
        } catch (Exception $e) {
            $test_result = "error";
            $error_message = "Error: " . $e->getMessage();
        }
    } else {
        $test_result = "error";
        $error_message = "Invalid email address provided.";
    }
}
?>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><?php echo $page_title; ?></title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px;
        }
        .container {
            max-width: 800px;
            margin: 0 auto;
            background: white;
            border-radius: 15px;
            box-shadow: 0 10px 40px rgba(0,0,0,0.2);
            overflow: hidden;
        }
        .header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 40px;
            text-align: center;
        }
        .header h1 {
            font-size: 2.5em;
            margin-bottom: 10px;
        }
        .header p {
            font-size: 1.1em;
            opacity: 0.9;
        }
        .content {
            padding: 40px;
        }
        .info-box {
            background: #f0f4ff;
            border-left: 4px solid #667eea;
            padding: 20px;
            margin: 20px 0;
            border-radius: 5px;
        }
        .info-box h3 {
            color: #667eea;
            margin-bottom: 10px;
        }
        .info-box table {
            width: 100%;
            margin-top: 10px;
        }
        .info-box td {
            padding: 8px 0;
        }
        .info-box td:first-child {
            font-weight: bold;
            width: 180px;
            color: #555;
        }
        .form-group {
            margin: 25px 0;
        }
        .form-group label {
            display: block;
            margin-bottom: 8px;
            font-weight: 600;
            color: #333;
        }
        .form-group input {
            width: 100%;
            padding: 12px;
            border: 2px solid #ddd;
            border-radius: 8px;
            font-size: 16px;
            transition: border-color 0.3s;
        }
        .form-group input:focus {
            outline: none;
            border-color: #667eea;
        }
        .btn {
            display: inline-block;
            padding: 15px 40px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border: none;
            border-radius: 8px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: transform 0.2s, box-shadow 0.2s;
            text-decoration: none;
        }
        .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 20px rgba(102, 126, 234, 0.4);
        }
        .btn-secondary {
            background: #6c757d;
            margin-left: 10px;
        }
        .alert {
            padding: 20px;
            border-radius: 8px;
            margin: 20px 0;
            font-weight: 500;
        }
        .alert-success {
            background: #d4edda;
            border: 1px solid #c3e6cb;
            color: #155724;
        }
        .alert-error {
            background: #f8d7da;
            border: 1px solid #f5c6cb;
            color: #721c24;
        }
        .alert-warning {
            background: #fff3cd;
            border: 1px solid #ffeaa7;
            color: #856404;
        }
        .warning-box {
            background: #fff3cd;
            border: 2px solid #ffc107;
            padding: 20px;
            border-radius: 8px;
            margin: 20px 0;
        }
        .warning-box strong {
            color: #856404;
            font-size: 1.1em;
        }
        .code {
            background: #f4f4f4;
            padding: 3px 8px;
            border-radius: 4px;
            font-family: 'Courier New', monospace;
            font-size: 0.9em;
        }
        .footer {
            background: #f8f9fa;
            padding: 20px;
            text-align: center;
            color: #666;
            border-top: 1px solid #dee2e6;
        }
        .links {
            margin-top: 20px;
            text-align: center;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>✉️ PHPMailer Test</h1>
            <p>Azure Communication Services Email Testing</p>
        </div>
        
        <div class="content">
            <div class="warning-box">
                <strong>⚠️ Security Warning:</strong> This is a test page. Remove this file in production environments!
            </div>
            
            <?php if ($test_result === 'success'): ?>
                <div class="alert alert-success">
                    <strong>✅ Success!</strong> Test email has been sent successfully. Check your inbox (and spam folder).
                </div>
            <?php elseif ($test_result === 'error'): ?>
                <div class="alert alert-error">
                    <strong>❌ Error!</strong> <?php echo htmlspecialchars($error_message); ?>
                </div>
            <?php endif; ?>
            
            <div class="info-box">
                <h3>📋 Current Configuration</h3>
                <table>
                    <tr>
                        <td>SMTP Host:</td>
                        <td><?php echo htmlspecialchars(AZURE_SMTP_HOST); ?></td>
                    </tr>
                    <tr>
                        <td>SMTP Port:</td>
                        <td><?php echo htmlspecialchars(AZURE_SMTP_PORT); ?></td>
                    </tr>
                    <tr>
                        <td>Encryption:</td>
                        <td><?php echo strtoupper(htmlspecialchars(AZURE_SMTP_ENCRYPTION)); ?></td>
                    </tr>
                    <tr>
                        <td>From Address:</td>
                        <td><?php echo htmlspecialchars(MAIL_FROM_ADDRESS); ?></td>
                    </tr>
                    <tr>
                        <td>From Name:</td>
                        <td><?php echo htmlspecialchars(MAIL_FROM_NAME); ?></td>
                    </tr>
                    <tr>
                        <td>Username:</td>
                        <td><?php echo htmlspecialchars(substr(AZURE_SMTP_USERNAME, 0, 20)) . '...'; ?></td>
                    </tr>
                    <tr>
                        <td>Log Path:</td>
                        <td><span class="code"><?php echo htmlspecialchars(MAIL_LOG_PATH); ?></span></td>
                    </tr>
                </table>
            </div>
            
            <form method="POST" action="">
                <div class="form-group">
                    <label for="recipient">📧 Recipient Email Address:</label>
                    <input 
                        type="email" 
                        id="recipient" 
                        name="recipient" 
                        placeholder="Enter email address to test"
                        value="<?php echo isset($_POST['recipient']) ? htmlspecialchars($_POST['recipient']) : '{{ test_email_recipient }}'; ?>"
                        required
                    >
                </div>
                
                <button type="submit" name="send_test" class="btn">
                    📤 Send Test Email
                </button>
            </form>
            
            <div class="info-box" style="margin-top: 30px;">
                <h3>📚 Next Steps</h3>
                <ul style="margin-left: 20px; margin-top: 10px; line-height: 1.8;">
                    <li>Check the recipient's inbox (and spam folder)</li>
                    <li>Review logs at <span class="code">{{ mail_log_path }}</span></li>
                    <li>See usage examples at <span class="code">/send_email_example.php</span></li>
                    <li>Configuration file: <span class="code">{{ phpmailer_install_dir }}/mailer_config.php</span></li>
                    <li><strong>Remove this test file before going to production!</strong></li>
                </ul>
            </div>
            
            <div class="links">
                <a href="/" class="btn btn-secondary">← Back to Home</a>
                <a href="/send_email_example.php" class="btn btn-secondary">View Examples →</a>
            </div>
        </div>
        
        <div class="footer">
            <p>PHPMailer with Azure Communication Services</p>
            <p style="margin-top: 5px; font-size: 0.9em;">Server: <?php echo htmlspecialchars(gethostname()); ?> | PHP <?php echo phpversion(); ?></p>
        </div>
    </div>
</body>
</html>
